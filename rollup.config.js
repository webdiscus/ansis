import copy from 'rollup-plugin-copy';
import cleanup from 'rollup-plugin-cleanup';
import { terser } from 'rollup-plugin-terser';

export default [
  {
    input: 'src/index.js',
    output: [
      {
        intro: '/* This file is auto generated by rollup.\nUse `npm run build` to create new CommonJS bundle. */',
        exports: 'named',
        // note: this bundle needs the wrapper `./dist/index.js` to properly export defaults
        file: './dist/bundle.js',
        format: 'cjs',
      },
    ],
    plugins: [
      // minify source and remove comments
      terser(),
      // copy files to dist
      copy({
        targets: [
          { src: 'package/*', dest: 'dist/' },
          { src: 'README.md', dest: 'dist/' },
          { src: 'LICENSE', dest: 'dist/' },
        ],
      }),
    ],
  },
  {
    input: 'src/index.dist.js',
    output: [
      {
        file: 'dist/index.js',
        format: 'cjs',
      },
    ],
    plugins: [
      // remove comments
      cleanup(),
    ],
  },
];
